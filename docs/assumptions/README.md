# docs/assumptions/

This directory holds the `Print Assumptions` output for every Rocq
theorem that `docs/FORMALIZATION_STATUS.md` advertises as
`CHECKED-EXACT`. One `<stem>.txt` file per theorem.

Files here are **generator output** produced by
`.github/scripts/print_assumptions.sh`. Do NOT edit them by hand. Add
a theorem to the generator's `AUDIT_LIST` when it reaches
`CHECKED-EXACT` in `FORMALIZATION_STATUS.md`, run the script, and
commit the resulting files.

CI runs the generator and diffs the freshly generated output against
what is committed — a mismatch fails the build. This is the
"assumptions have been captured and reviewed" requirement of the
status vocabulary made mechanical: the assumption footprint of every
advertised checked theorem is visible in the tree and moves in
lockstep with the source.

## Current contents

Five reports, one per theorem the correspondence table classifies as
`CHECKED-RESTRICTED`. They were generated by the CI run for commit
`e478307` (build ✅, `coqchk` ✅, audit ✅).

Nothing is `CHECKED-EXACT` yet, so nothing here backs a
machine-checked v3 theorem claim. `DEFINITION-EXACT` rows
(Def 4.1, Def 4.3, Def 6.4) get no file — a definition has no
`Print Assumptions` in the interesting sense.

## What the reports say

All five verdicts are **identical**:

```
Axioms:
ClassicalDedekindReals.sig_forall_dec
FunctionalExtensionality.functional_extensionality_dep
```

Both are inherent to Rocq's stdlib construction of the classical real
numbers, and are exactly the kind of dependency the status document
permits ("axioms inherent in Stdlib+MathComp"). **No legacy axiom
appears** — nothing from `Adjunction/Functors.v` (`find_index_*`),
`Examples/ChebyshevProof.v` (`rolle`,
`chebyshev_nodal_identity_axiom`), `ErrorBound.v`
(`parseval_identity_integration`), or `Example.v`
(`error_bound_example`). The `Coq/V3/` tree is clean of the legacy
axiom surface.

Note that the footprint is uniform even for theorems whose proofs use
no classical reasoning at all (`prop_3_3_lower_bound` is one `apply`).
The reason is structural rather than proof-specific: the
`V3_Presentation.Presentation` record carries `distF : F -> F -> R`
and metric-axiom fields typed in `R`, so *any* statement quantifying
over a presentation mentions `R`, whose stdlib definition rests on
those two axioms. Consequently no V3 theorem can be axiom-free while
the analytic distance lands in `R`. Removing them would require
re-basing the development on a constructive reals library, which is a
deliberate design decision and not currently proposed.
